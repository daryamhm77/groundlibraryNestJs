export enum OrderStatus {
  Pending = 'pending',
  Canceled = 'canceled',
  Paid = 'Paid',
  Ordered = 'ordered',
  InProcess = 'in-process',
  Packed = 'Packed',
  InTransit = 'in-transit',
  Delivered = 'delivered',
}
export enum OrderItemStatus {
  Pending = 'pending',
  Canceled = 'canceled',
  Sent = 'sent',
}
